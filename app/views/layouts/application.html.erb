<!DOCTYPE html>
<html>
<head>
  <title>NinetyNineCats</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>



<body>

  <div class="container">
    <div class="header">
      <ul class="nav nav-pills pull-right">
        
        

        
        <li class="<%= 'active' if cats_url == request.original_url %>">
          <a href="<%= cats_url %>">
            Cats
          </a>
        </li>
        

        
       
        <% if current_user %>
        
          <li class="<%= 'active' if cat_rental_requests_url == request.original_url %>">
            <a href="<%= cat_rental_requests_url %>">
              Requests
            </a>
          </li>
        
          <li class="<%= 'active' if new_cat_url == request.original_url %>">
            <a href="<%= new_cat_url %>">
              Design Cat
            </a>
          </li>
        
          <li class="<%= 'active' if new_cat_rental_request_url == request.original_url %>">
            <a href="<%= new_cat_rental_request_url %>">
              Request Cat
            </a>
          </li>
                  
          <li><%= link_to( "Log off", log_off_url, method: :delete ) %></li>
        <% else %>
          <li class="<%= 'active' if new_user_url == request.original_url %>">
            <a href="<%= new_user_url %>">
              Sign Up
            </a>
          </li>
        
          <li
            class="<%= 'active' if new_session_url == request.original_url %>">
              <%= link_to( "Log on", new_session_url ) %>
          </li>
        <% end %>
        
      </ul>
      <h3 class="text-muted"><a href= '<%= cats_url%>'>Cat Futures</a></h3>
    </div>
    
    <h2><%= @title %></h2>
    
    <% unless flash.empty? %>
      <% flash.each do |key, msgs| %>
        <% msgs.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      <% end %>
   <%end %>
   
    <%= yield %>

    <br><br>
    <div class="footer">
      <p>&copy; CatsAccademy 2025</p>
    </div>

  </div> <!-- /container -->


  <!-- Bootstrap core JavaScript
  ================================================== -->
  <!-- Placed at the end of the document so the pages load faster -->
</body>
</html>


</html>
